'use strict';

import Game from "./Game";

const BrutalTD = new Game();

// let level_btn = document.querySelectorAll('.load_level');

// for(let b of level_btn) {
// 	b.addEventListener("click", (e) => BrutalTD.start("sample", e.target.getAttribute("data-level")) );
// }

// document.querySelector("#send_wave").addEventListener('click', () => BrutalTD.send_wave());